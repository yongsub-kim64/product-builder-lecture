---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { games, type Game } from '../../data/games';

export function getStaticPaths() {
  return games.map(game => ({
    params: { game: game.id },
    props: { game },
  }));
}

interface Props {
  game: Game;
}

const { game } = Astro.props;
const gameUrl = `/games/${game.id}/index.html`;
---

<BaseLayout
  title={game.title}
  titleOverride={`${game.title} | Vibe Coding Playground — chulbuji.com`}
  description={game.descriptionKo}
  noindex={true}
>
  <div class="gp-layout">

    <div class="gp-topbar">
      <div class="gp-topbar__left">
        <a href="/playground" class="gp-back">← Playground</a>
        <span class="gp-title">{game.title}</span>
      </div>
      <div class="gp-topbar__actions">
        {game.insightSlug ? (
          <a href={`/insight/${game.insightSlug}`} class="gp-btn gp-btn--ghost">
            📝 제작기
          </a>
        ) : (
          <span class="gp-btn gp-btn--disabled" title="준비 중입니다">
            📝 제작기 (준비중)
          </span>
        )}
        <a href={gameUrl} target="_blank" rel="noopener" class="gp-btn gp-btn--ghost">
          ↗ 새 탭
        </a>
        <button id="fullscreen-btn" class="gp-btn gp-btn--primary">
          ⛶ 전체화면
        </button>
      </div>
    </div>

    <div class="gp-frame-wrapper" id="frame-wrapper">
      <iframe
        id="game-frame"
        src={gameUrl}
        title={game.title}
        allow="fullscreen"
        scrolling="no"
      ></iframe>
    </div>

  </div>
</BaseLayout>

<style>
  /* 게임 페이지 전체를 뷰포트에 맞게 */
  .gp-layout {
    display: flex;
    flex-direction: column;
    height: calc(100svh - var(--header-h, 62px));
  }

  /* 상단 바 */
  .gp-topbar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 1.25rem;
    height: 52px;
    flex-shrink: 0;
    background: #fff;
    border-bottom: 1px solid #E5E7EB;
    gap: 1rem;
  }

  .gp-topbar__left {
    display: flex;
    align-items: center;
    gap: 1rem;
    min-width: 0;
  }

  .gp-back {
    font-size: 0.875rem;
    color: #6B7280;
    text-decoration: none;
    white-space: nowrap;
    transition: color 0.2s;
  }

  .gp-back:hover {
    color: #1E3A8A;
  }

  .gp-title {
    font-size: 1rem;
    font-weight: 700;
    color: #111827;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .gp-topbar__actions {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex-shrink: 0;
  }

  .gp-btn {
    display: inline-flex;
    align-items: center;
    padding: 0.35rem 0.8rem;
    border-radius: 6px;
    font-size: 0.8rem;
    font-weight: 600;
    white-space: nowrap;
    cursor: pointer;
    text-decoration: none;
    border: none;
    transition: all 0.2s;
  }

  .gp-btn--primary {
    background: #1E3A8A;
    color: #fff;
  }

  .gp-btn--primary:hover {
    background: #1e40af;
  }

  .gp-btn--ghost {
    background: transparent;
    color: #374151;
    border: 1px solid #D1D5DB;
  }

  .gp-btn--ghost:hover {
    border-color: #1E3A8A;
    color: #1E3A8A;
  }

  .gp-btn--disabled {
    background: transparent;
    color: #9CA3AF;
    border: 1px solid #E5E7EB;
    cursor: not-allowed;
    font-size: 0.8rem;
    font-weight: 500;
    padding: 0.35rem 0.8rem;
    border-radius: 6px;
    white-space: nowrap;
  }

  /* iframe 래퍼 */
  .gp-frame-wrapper {
    flex: 1;
    overflow: hidden;
    background: #000;
    /* 터치 스크롤이 iframe 배경에 의해 방해되지 않도록 */
    touch-action: none;
  }

  #game-frame {
    width: 100%;
    height: 100%;
    border: none;
    display: block;
  }

  @media (max-width: 480px) {
    .gp-topbar {
      padding: 0 0.75rem;
      height: 48px;
    }

    .gp-btn--disabled {
      display: none;
    }

    .gp-title {
      font-size: 0.9rem;
    }
  }
</style>

<script>
  // 실제 헤더 높이를 측정해서 레이아웃 높이 보정
  function setHeaderHeight() {
    const header = document.querySelector('.site-header') as HTMLElement | null;
    if (header) {
      document.documentElement.style.setProperty(
        '--header-h',
        header.offsetHeight + 'px'
      );
    }
  }
  setHeaderHeight();
  window.addEventListener('resize', setHeaderHeight);

  // 전체화면 버튼
  const fullscreenBtn = document.getElementById('fullscreen-btn');
  const gameFrame = document.getElementById('game-frame') as HTMLIFrameElement | null;

  fullscreenBtn?.addEventListener('click', () => {
    if (gameFrame?.requestFullscreen) {
      gameFrame.requestFullscreen();
    }
  });

  // 모바일: iframe 위 터치가 페이지 스크롤 막기
  const wrapper = document.getElementById('frame-wrapper');
  wrapper?.addEventListener('touchmove', (e) => {
    e.preventDefault();
  }, { passive: false });
</script>
