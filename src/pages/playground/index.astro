---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { games } from '../../data/games';
---

<BaseLayout
  title="Playground"
  titleOverride="Vibe Coding Playground | chulbuji.com"
  description="AI와 협업하여 만든 1-file 데모 공간. Breakout, Tetris, Snake 등 바이브 코딩 결과물을 직접 체험해보세요."
>
  <div class="pg-page">

    <header class="pg-hero">
      <h1 class="pg-hero__title">Vibe Coding Playground</h1>
      <p class="pg-hero__sub">AI와 협업하여 만든 1-file 데모 공간입니다</p>
      <p class="pg-hero__desc">
        복잡한 셋업 없이, 단 하나의 HTML 파일로 완성한 바이브 코딩 결과물들입니다.<br>
        각 데모는 Claude와의 협업 세션에서 탄생했습니다.
      </p>
    </header>

    <section class="pg-grid">
      {games.map(game => (
        <article class="pg-card">
          <div class="pg-card__thumb" data-game={game.id}>
            {game.thumbnail
              ? <img src={game.thumbnail} alt={game.title} loading="lazy" decoding="async" />
              : <span class="pg-card__thumb-emoji">{game.emoji}</span>
            }
          </div>
          <div class="pg-card__inner">
            <div class="pg-card__body">
              <h2 class="pg-card__title">{game.title}</h2>
              <p class="pg-card__desc">{game.descriptionKo}</p>
              <div class="pg-card__tags">
                {game.tags.map(tag => (
                  <span class="pg-tag">{tag}</span>
                ))}
              </div>
            </div>
            <div class="pg-card__foot">
              <a href={`/playground/${game.id}`} class="pg-btn-play">
                Play Now →
              </a>
            </div>
          </div>
        </article>
      ))}
    </section>

  </div>
</BaseLayout>

<style>
  .pg-page {
    max-width: 1100px;
    margin: 0 auto;
    padding: 3rem 1.5rem 5rem;
  }

  /* Hero */
  .pg-hero {
    text-align: center;
    margin-bottom: 3.5rem;
  }

  .pg-hero__title {
    font-size: clamp(2rem, 1.6rem + 2vw, 3rem);
    font-weight: 800;
    color: #111827;
    margin-bottom: 0.75rem;
    letter-spacing: -0.02em;
  }

  .pg-hero__sub {
    font-size: clamp(1rem, 0.9rem + 0.5vw, 1.2rem);
    color: #4B5563;
    margin-bottom: 1rem;
    font-weight: 500;
  }

  .pg-hero__desc {
    font-size: 0.95rem;
    color: #6B7280;
    line-height: 1.7;
    max-width: 560px;
    margin: 0 auto;
  }

  /* Grid */
  .pg-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 1.5rem;
  }

  /* Card */
  .pg-card {
    background: #fff;
    border: 1px solid #E5E7EB;
    border-radius: 16px;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    transition: box-shadow 0.2s, transform 0.2s, border-color 0.2s;
  }

  .pg-card:hover {
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.09);
    transform: translateY(-3px);
    border-color: #C7D2FE;
  }

  /* Thumbnail */
  .pg-card__thumb {
    width: 100%;
    aspect-ratio: 16 / 9;
    overflow: hidden;
    flex-shrink: 0;
    background: linear-gradient(135deg, #1e3a8a 0%, #2563eb 60%, #3b82f6 100%);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .pg-card__thumb[data-game="tetris"] {
    background: linear-gradient(135deg, #5b21b6 0%, #7c3aed 60%, #a78bfa 100%);
  }

  .pg-card__thumb[data-game="snake"] {
    background: linear-gradient(135deg, #065f46 0%, #059669 60%, #34d399 100%);
  }

  .pg-card__thumb img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.35s ease;
  }

  .pg-card:hover .pg-card__thumb img {
    transform: scale(1.04);
  }

  .pg-card__thumb-emoji {
    font-size: 3.5rem;
    line-height: 1;
    opacity: 0.85;
    transition: transform 0.3s ease;
  }

  .pg-card:hover .pg-card__thumb-emoji {
    transform: scale(1.12);
  }

  /* Card inner */
  .pg-card__inner {
    padding: 1.25rem 1.75rem 1.75rem;
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    flex: 1;
  }

  .pg-card__body {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .pg-card__title {
    font-size: 1.4rem;
    font-weight: 700;
    color: #111827;
    margin: 0;
  }

  .pg-card__desc {
    font-size: 0.9rem;
    color: #6B7280;
    line-height: 1.6;
    margin: 0;
  }

  .pg-card__tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
    margin-top: 0.25rem;
  }

  .pg-tag {
    display: inline-block;
    padding: 0.2rem 0.65rem;
    background: #F3F4F6;
    color: #374151;
    font-size: 0.75rem;
    font-weight: 500;
    border-radius: 999px;
  }

  .pg-card__foot {
    display: flex;
    justify-content: flex-end;
  }

  .pg-btn-play {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    padding: 0.6rem 1.25rem;
    background: #1E3A8A;
    color: #fff;
    font-size: 0.9rem;
    font-weight: 600;
    border-radius: 8px;
    text-decoration: none;
    transition: background 0.2s;
  }

  .pg-btn-play:hover {
    background: #1e40af;
  }

  @media (max-width: 640px) {
    .pg-page {
      padding: 2rem 1rem 4rem;
    }

    .pg-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
